# POS Backend (Node.js + Express + MongoDB)

This is the backend for the POS (Point of Sale) system built using Node.js, Express, and MongoDB.

---

## 🚀 Setup Instructions

### 1. Clone or create the project

bash
git clone <your-repo-url>
cd pos-backend
